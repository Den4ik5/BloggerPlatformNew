{% extends 'test/mainMenu.html.twig' %}

{% block head %}
    {{ parent() }}
    {% block title %}{% endblock %}
{% endblock %}

    {% block content%}
        {{ parent() }}
        {{ email }}
        {{ message }}

        <a href="{{ path('registration') }}">You can register new account using this email!</a>
        <a href="{{ path('login') }}">You can log in using correct email</a>
    {% endblock content %}

