{% if errors %}
    <div>{{ errors.messageKey|trans(errors.messageData, 'security') }}</div>
{% endif %}
{% if form %}

    <!--<input type="hidden" name="_target_path" value="'{'{ path('test') }}" /><!--account-->
    <!--<input type="hidden" name="_failure_path" value="{'{' path('test') }}" /><!--forgot_password-->
    <!--<input type="hidden" name="go_to" value="{'{ path('test') }}" /><!--dashboard-->
    <!--<input type="hidden" name="back_to" value="{'{ path('test') }}" /><!--forgot_password-->


{{ form_start(form,{'attr': {'id':'login-form'}}) }}
<div class="form-group">
    {{ form_label(form._username) }}
    {{ form_widget(form._username,{'attr':{'class':'form-control'}})  }}
</div>
<div class="form-group">
    {{ form_label(form._password) }}
    {{ form_widget(form._password,{'attr':{'class':'form-control'}}) }}
</div>
<div class="form-group text-center">
    <input type="checkbox" id="remember_me" name="_remember_me" checked />
    <label for="remember_me">Remember Me</label>
</div>
<div class="form-group">
    <div class="row">
        <div class="col-sm-6 col-sm-offset-3">

            <input type="submit" name="login-submit" value="Log in" id="login-submit" title="Log in" tabindex="4" class="form-control">
            <input type="hidden" name="_target_path" value="{{ path('test') }}" >
            <input type="hidden" name="_target_path" value="{{ path('test') }}" >
        </div>
    </div>
</div>
<a href="{{ path('registration') }}"> Have not registrated yet? Just do it!</a>
{% endif %}