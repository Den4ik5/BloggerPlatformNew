<div class="media text-muted pt-3">
    <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
<span class="d-block">
<a href="{{ path('user_post', {'username': post.postCreator.email}) }}">
<strong class="darken-1">by {{ post.postCreator.username }}</strong>
</a>
</span>
        <a href="{{ path('post_show', {'id': post.id}) }}">
            {{ post.content }}
        </a>
    </p>
</div>